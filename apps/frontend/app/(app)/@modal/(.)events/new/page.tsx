import { NewEventDialog } from "../../../events/new/new-event-dialog";

/** Abgefangene Route: "Neues Event" öffnet sich als Overlay über der Übersicht. */
export default function NewEventModal() {
  return <NewEventDialog />;
}
